Requires a reasonably recent version of Emscripten to run.
On both Linux and Windows:

Enable emscripten in the local terminal:
`emsdk activate`

Build WASM components:
`build.bat; cd ..`

Start local web server:
`emrun index.html`